"use strict";
// Эти методы заполняют текущую строку указанным символом до указанной длины.
// Если строка меньше указанной длины то она просто возвращается как есть, без изменения.
console.log("Hello".padEnd(10) + "!");
console.log("Hello".padStart(10) + "!");
