/**
 * Export all repositories
 */

const DeviceRepository = require('./DeviceRepository');
const SensorReadingRepository = require('./SensorReadingRepository');

module.exports = {
  DeviceRepository,
  SensorReadingRepository
};
